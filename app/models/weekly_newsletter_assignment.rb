class WeeklyNewsletterAssignment < ActiveRecord::Base
	# belongs_to :weekly_newsletter, touch: true
	# validates_presence_of :weekly_newsletter

	# belongs_to :taggable, polymorphic: true, touch: true
	# validates_presence_of :taggable
end
